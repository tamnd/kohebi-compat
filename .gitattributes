# The corpus is bytes, not text. A file in it exists to test how the tokenizer
# handles the exact line endings it was written with, so git must not normalise
# them on the way in or translate them on the way out.
corpus/local/** -text
